package act.db.filter;

/**
 * A `FilterPart` is a discrete part of a filter defines database records/rows that
 * is impact by a certain DB operations.
 *
 * A `FilterPart` is composed of
 * -
 */
public interface FilterPart {
}
