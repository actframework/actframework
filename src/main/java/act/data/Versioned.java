package act.data;

/**
 * A `Versioned` data has method to retrieve the version
 */
public interface Versioned {
    /**
     * Returns the version of the data
     * @return the version
     */
    String _version();
}
