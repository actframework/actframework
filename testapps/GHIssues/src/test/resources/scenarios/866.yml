Scenario(866):
  description: "`PropertySpec` specification not working when return value is a list"
  interactions:
    - description: test
      request:
        accept: application/json
        get: 866
      response:
        json:
          size: 1
          0:
            name:
              - exists: true
            number:
              - exists: false