Scenario(797):
  description: "JSON output: default format for `java.util.Date` missing time part"
  interactions:
  - description: Test
    request:
      get: 797
    response:
      ${sizeOf(date)}:
        - gt: 15
