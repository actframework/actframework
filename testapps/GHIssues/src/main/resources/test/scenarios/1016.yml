Scenario(1016):
  description: "[1016] Annotations with ValueLoader cannot work if post with json"
  interactions:
  - description: Test
    request:
      post: /1016
      json:
        id: 1
    response:
      json:
        id: 1
        ip:
          - exists: true
