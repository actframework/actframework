Scenario(784):
  description: "`@Data` enhancement error when Model class has no fields"
  interactions:
  - description: test
    request:
      get: 784
    response:
      json:
        result:
        - ne: 0
