Scenario(958):
  description: "[GH958] `NullPointerException` when route path element wrapped with `~` is no the final one"
  interactions:
  - description: test
    request:
      get: 958/foo-bar
    response:
      json:
        result: ok
