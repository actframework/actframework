Scenario(798):
  description: "`@JSONField(format = 'yyyy-MM')` setting not work"
  interactions:
  - description: Test
    request:
      get: 798
    response:
      json:
        ${sizeOf(date)}: 7
