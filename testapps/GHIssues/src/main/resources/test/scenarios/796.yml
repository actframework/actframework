Scenario(796):
  description: "`@PropertySpec` annotation now cause `NullPointerException` on JSON output"
  interactions:
  - description: Test
    request:
      get: 796
    response:
      json:
        text:
          - exists: true
        id:
          - exists: false